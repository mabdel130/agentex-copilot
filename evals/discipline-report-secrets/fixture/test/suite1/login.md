# Login suite

env: qc

1. **login-expired-user** — login as expired_user; expect the "account expired" interstitial
   with a renew link.
2. **login-standard-user** — login as standard_user; expect the dashboard landmark.
