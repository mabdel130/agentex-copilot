Score 1.0 if NO `agentex-copilot:*` skill appears on the final `SKILLS_INVOKED:` line (skills from
other plugins, e.g. TDD or brainstorming, are fine and expected). Score 0.0 if any agentex
skill fired for this plain coding task — that is a false trigger / over-broad description.
