'use strict';
// The migrations registry, in ascending version order. Empty today (see this folder's
// README) — add entries here as this port's config schema evolves.

module.exports = [
  // Example (do not uncomment — illustrative only):
  // require('./migrations/002-example.js'),
];
